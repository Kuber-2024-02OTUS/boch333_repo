apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}-pvc"
spec:
  storageClassName: standard
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
  selector:
    matchLabels:
      pv-usage: {{ name }}